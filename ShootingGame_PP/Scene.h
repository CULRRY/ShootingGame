#pragma once
enum class SceneType
{
	START,
	STAGE,
	LOAD,
	GAME_OVER,
	GAME_CLEAR,
	PAUSE,
};

struct Position
{
	int32 y;
	int32 x;
};

using Cursor = Position;

extern SceneType gScene;
extern Cursor gCursor;
extern int32 gStageLevel;
extern bool gExit;

//-------------------------------------------------------------------
/// @brief 커서 위치를 초기화한다.
//-------------------------------------------------------------------
void InitCursor();

//-------------------------------------------------------------------
/// @brief 커서를 버퍼에 그린다.
//-------------------------------------------------------------------
void DrawCursor();

//-------------------------------------------------------------------
/// @brief 스타트 화면을 버퍼에 그린다.
//-------------------------------------------------------------------
void DrawStartScene();

//-------------------------------------------------------------------
/// @brief 일시정지 화면을 버퍼에 그린다.
//-------------------------------------------------------------------
void DrawPauseScene();

//-------------------------------------------------------------------
/// @brief 게임오버 화면을 버퍼에 그린다.
//-------------------------------------------------------------------
void DrawGameOverScene();

//-------------------------------------------------------------------
/// @brief 게임클리어 화면을 버퍼에 그린다.
//-------------------------------------------------------------------
void DrawGameClearScene();